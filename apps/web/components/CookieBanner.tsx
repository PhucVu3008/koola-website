/**
 * Cookie consent banner — currently hidden per product decision.
 * Preserved for future use if legal requirements change.
 */
export function CookieBanner({ locale: _locale }: { locale: string }) {
  // Hidden: cookie consent not required for current usage
  return null;
}
