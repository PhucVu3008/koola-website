# Frontend (Next.js)
# In Docker Compose, this is set automatically for the `web` service.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Google Analytics 4
# Measurement ID for koola.vn (GA4)
# Leave empty to disable GA in local development
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-Q1HY3MEZPC
