/**
 * Design tokens used across the marketing site.
 *
 * These values are intentionally simple so they can back Tailwind utilities
 * while remaining easy to update in one place.
 */
export const TOKENS = {
  containerMaxWidth: 1240,
  sectionGapY: '72px',
} as const;
